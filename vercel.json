{
  "buildCommand": "prisma generate && npm run build",
  "framework": null,
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/generate-invoices",
      "schedule": "0 2 * * *"
    }
  ]
}

